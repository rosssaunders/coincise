name: Gate.io Docs Update

on:
  schedule:
    # Run every day at 00:00 UTC
    - cron: "0 0 * * *"
  workflow_dispatch: # Allow manual triggering
  push:
    paths:
      - "venues/gateio/**"

jobs:
  update-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    defaults:
      run:
        working-directory: venues/gateio

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history for git diff

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Install Puppeteer browsers
        run: pnpm exec puppeteer browsers install chrome


          popd

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Install Puppeteer browsers
        run: pnpm exec puppeteer browsers install chrome


      - name: Run extract:change-log
        run: pnpm run extract:change-log

      - name: Run extract:general
        run: pnpm run extract:general

      - name: Run extract:account
        run: pnpm run extract:account

      - name: Run extract:subaccount
        run: pnpm run extract:subaccount

      - name: Run extract:collateral-loan
        run: pnpm run extract:collateral-loan

      - name: Run extract:multi-collateral-loan
        run: pnpm run extract:multi-collateral-loan

      - name: Run extract:isolated-margin
        run: pnpm run extract:isolated-margin

      - name: Run extract:earn
        run: pnpm run extract:earn

      - name: Run extract:earnuni
        run: pnpm run extract:earnuni

      - name: Run extract:flash-swap
        run: pnpm run extract:flash-swap

      - name: Run extract:rebate
        run: pnpm run extract:rebate

      - name: Run extract:wallet
        run: pnpm run extract:wallet

      - name: Run extract:withdrawal
        run: pnpm run extract:withdrawal

      - name: Run extract:unified
        run: pnpm run extract:unified

      - name: Run extract:spot
        run: pnpm run extract:spot

      - name: Run extract:options
        run: pnpm run extract:options

      - name: Run extract:futures
        run: pnpm run extract:futures

      - name: Run extract:delivery
        run: pnpm run extract:delivery

      - name: Run extract:websocket-spot
        run: pnpm run extract:websocket-spot

      - name: Run extract:websocket-delivery
        run: pnpm run extract:websocket-delivery

      - name: Run extract:websocket-futures
        run: pnpm run extract:websocket-futures

      - name: Run extract:websocket-options
        run: pnpm run extract:websocket-options

      - name: Run extract:websocket-unified
        run: pnpm run extract:websocket-unified


      - name: Format documentation
        run: |
          cd ../..
          ./formatdocs.sh docs/gateio/
      - name: Check for changes
        id: git-check
        run: |
          cd ../..  # Move back to root directory for git operations
          # Explicitly add the docs/gateio directory to staging
          git add docs/gateio
          # Check if there are any changes
          git diff --staged --quiet || echo "changes=true" >> $GITHUB_OUTPUT

      - name: Generate AI PR Summary
        if: steps.git-check.outputs.changes == 'true'
        id: ai-summary
        run: |
          cd ../..  # Move back to root directory for git operations
          # Generate AI summary using the AI PR summary tool (max diff length: 15000)
          SUMMARY=$(cd tools/ai-pr-summary && pnpm run generate-pr-summary ../../ 15000 2>/dev/null | sed -n '/---PR_SUMMARY_START---/,/---PR_SUMMARY_END---/p' | sed '1d;$d')
          # Escape the summary for GitHub Actions
          echo "summary<<EOF" >> $GITHUB_OUTPUT
          echo "$SUMMARY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Create Pull Request
        if: steps.git-check.outputs.changes == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "docs: update Gate.io API documentation"
          title: "Gate.io API Documentation Update"
          branch: "gateio-docs-update"
          base: "main"
          delete-branch: true
          token: ${{ secrets.PAT_GITHUB }}
          body: ${{ steps.ai-summary.outputs.summary }}
          reviewers: "@rosssaunders"
          labels: "auto-docs-update"
