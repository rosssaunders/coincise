name: Weekly Binance Spot Documentation Update

on:
  schedule:
    # Run every Sunday at 00:00 UTC
    - cron: '0 0 * * 0'
  workflow_dispatch: # Allow manual triggering

jobs:
  update-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    defaults:
      run:
        working-directory: venues/binancespot

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history for git diff

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: venues/binancespot/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run Binance Spot scraper (public_rest)
        run: npm run start -- ./config/public_rest.json

      - name: Run Binance Spot scraper (private_rest)
        run: npm run start -- ./config/private_rest.json

      - name: Run Binance Spot scraper (public_websocket)
        run: npm run start -- ./config/public_websocket.json

      - name: Run Binance Spot scraper (private_websocket)
        run: npm run start -- ./config/private_websocket.json

      - name: Run Binance Spot scraper (fix)
        run: npm run start -- ./config/fix.json

      - name: Run Binance Spot scraper (sbe)
        run: npm run start -- ./config/sbe.json

      - name: Check for changes
        id: git-check
        run: |
          cd ../..  # Move back to root directory for git operations
          # Explicitly add the docs/binance directory to staging
          git add docs/binance
          # Check if there are any changes
          git diff --staged --quiet || echo "changes=true" >> $GITHUB_OUTPUT

      - name: Create Pull Request
        if: steps.git-check.outputs.changes == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: 'docs: update Binance API documentation'
          title: 'Weekly Binance Spot API Documentation Update'
          body: 'Automated weekly update of Binance API documentation'
          branch: 'binance-docs-update'
          base: 'main'
          delete-branch: true
          token: ${{ secrets.PAT_GITHUB }}
