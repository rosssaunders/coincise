# Response Formats

All responses from the OKX API are in JSON format.

Successful responses contain a "code" field with value "0" and a "data" field
with the response payload.

Error responses contain a "code" field with an error code and a "msg" field with
the error message.
